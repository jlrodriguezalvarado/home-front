# Deploy — Home Manager PWA (Angular)

La guía de deploy de este repo está en la raíz:

**[`../DEPLOY.md`](../DEPLOY.md)**

Ahí están: `environment.prod.ts`, build `dist/`, imagen solo-`dist`, publicación
vía el stack de `home-api`, verificación SPA/PWA y rollback de `WEB_VERSION`.
