# Copy to .env.docker (gitignored) before building the production web image.
# Angular is built on the host; this file only configures the Docker image pack.
FRONT_DIST_PATH=dist/home-manager/browser
REGISTRY_HOST=registry.lumuscore.com
WEB_IMAGE=home/web
# Optional override; default is short git SHA
# WEB_VERSION=
